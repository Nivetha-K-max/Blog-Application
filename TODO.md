# TODO

- [ ] Update `backend/src/main/java/com/example/blog/config/DataSeeder.java` to ensure 1 admin + 5 users (known creds) and at least 5 blog posts tied to those users.
- [ ] Update `frontend/src/styles.css` background-image URLs for login/register/admin pages.
- [ ] Update seed verification by restarting backend with an empty DB (or by changing DataSeeder to also seed blogs when blogs are empty).

- [ ] Run frontend build/reload (port 5175) and verify backgrounds + seeded posts/users work.

